<div class="min-h-screen bg-white">
    @if($user->role === 'admin')
        <!-- Redirect admin to admin dashboard -->
        <script>window.location.href = "{{ route('admin.dashboard') }}";</script>
    @else
        <!-- E-Commerce Style Public Dashboard (Shopee-inspired) -->
        
        <!-- Top Bar with Search -->
        <div class="bg-gradient-to-r from-orange-500 to-orange-600 text-white">
            <div class="max-w-7xl mx-auto px-4 py-3 flex flex-col md:flex-row md:items-center md:justify-between gap-3">
                <div class="flex items-center gap-2 text-sm">
                    <span>🛍️ SentriPay Mall</span>
                    <span class="opacity-80">• Promo Harian • Gratis Ongkir</span>
                </div>
                <form action="{{ route('products') }}" method="GET" class="w-full md:w-1/2">
                    <label class="sr-only" for="search">Cari</label>
                    <div class="relative">
                        <input id="search" name="search" type="text" placeholder="Cari produk, brand, atau kategori" class="w-full rounded-full border-none px-4 py-2 text-gray-800 focus:ring-2 focus:ring-orange-400" />
                        <button type="submit" class="absolute right-1 top-1 bottom-1 px-4 rounded-full bg-orange-600 text-white font-semibold hover:bg-orange-700 transition">Cari</button>
                    </div>
                </form>
                <p class="text-sm opacity-90">Halo, {{ $user->name }}</p>
            </div>
        </div>

        <!-- Promotional Slider -->
        <div class="bg-[#fef6f1]">
            <div class="max-w-7xl mx-auto px-4 py-8">
                <div class="relative overflow-hidden rounded-2xl shadow-xl" x-data="{ currentSlide: 0, slides: 3 }" x-init="setInterval(() => { currentSlide = (currentSlide + 1) % slides }, 4500)">
                    <!-- Slide 1 -->
                    <div x-show="currentSlide === 0" x-transition class="relative h-96 bg-gradient-to-r from-orange-500 via-orange-400 to-amber-400">
                        <div class="absolute inset-0 flex items-center">
                            <div class="max-w-7xl mx-auto px-8 w-full">
                                <div class="max-w-xl">
                                    <div class="inline-flex items-center gap-2 bg-white/15 border border-white/30 text-white px-3 py-1 rounded-full text-sm mb-3">Flash Sale • 12.12</div>
                                    <h2 class="text-5xl font-bold text-white mb-4">Diskon Besar Setiap Hari</h2>
                                    <p class="text-lg text-white/90 mb-6">Serbu produk pilihan dengan harga miring dan gratis ongkir.</p>
                                    <a href="{{ route('products') }}" class="inline-block bg-white text-orange-600 px-8 py-3 rounded-full font-bold hover:bg-gray-100 transition">
                                        Belanja Sekarang
                                    </a>
                                </div>
                            </div>
                        </div>
                        <div class="absolute right-0 bottom-0 w-1/2 h-full opacity-15">
                            <svg class="w-full h-full" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
                            </svg>
                        </div>
                    </div>

                    <!-- Slide 2 -->
                    <div x-show="currentSlide === 1" x-transition class="relative h-96 bg-gradient-to-r from-rose-500 via-pink-500 to-orange-400">
                        <div class="absolute inset-0 flex items-center">
                            <div class="max-w-7xl mx-auto px-8 w-full">
                                <div class="max-w-xl">
                                    <div class="inline-flex items-center gap-2 bg-white/15 border border-white/30 text-white px-3 py-1 rounded-full text-sm mb-3">Brand Pilihan</div>
                                    <h2 class="text-5xl font-bold text-white mb-4">Produk Baru Tiap Minggu</h2>
                                    <p class="text-lg text-white/90 mb-6">Jelajahi koleksi terbaru dari penjual tepercaya.</p>
                                    <a href="{{ route('products') }}" class="inline-block bg-white text-rose-600 px-8 py-3 rounded-full font-bold hover:bg-gray-100 transition">
                                        Lihat Koleksi
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Slide 3 -->
                    <div x-show="currentSlide === 2" x-transition class="relative h-96 bg-gradient-to-r from-amber-500 via-orange-500 to-red-500">
                        <div class="absolute inset-0 flex items-center">
                            <div class="max-w-7xl mx-auto px-8 w-full">
                                <div class="max-w-xl">
                                    <div class="inline-flex items-center gap-2 bg-white/15 border border-white/30 text-white px-3 py-1 rounded-full text-sm mb-3">Seller Center</div>
                                    <h2 class="text-5xl font-bold text-white mb-4">Jual Produkmu di SentriPay</h2>
                                    <p class="text-lg text-white/90 mb-6">Buka toko gratis, upload produk, dan mulai terima pesanan.</p>
                                    <a href="{{ route('seller.product.create') }}" class="inline-block bg-white text-orange-600 px-8 py-3 rounded-full font-bold hover:bg-gray-100 transition">
                                        Mulai Jual
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Slider Navigation -->
                    <div class="absolute bottom-4 left-1/2 transform -translate-x-1/2 flex gap-2">
                        <button @click="currentSlide = 0" :class="currentSlide === 0 ? 'bg-white' : 'bg-white/50'" class="w-3 h-3 rounded-full transition"></button>
                        <button @click="currentSlide = 1" :class="currentSlide === 1 ? 'bg-white' : 'bg-white/50'" class="w-3 h-3 rounded-full transition"></button>
                        <button @click="currentSlide = 2" :class="currentSlide === 2 ? 'bg-white' : 'bg-white/50'" class="w-3 h-3 rounded-full transition"></button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Categories Quick Access -->
        @if($categories->count() > 0)
        <div class="bg-white border-b">
            <div class="max-w-7xl mx-auto px-4 py-6">
                <h3 class="text-lg font-bold text-gray-900 mb-4">Kategori Populer</h3>
                <div class="flex flex-wrap gap-3">
                    @foreach($categories as $category)
                    <a href="{{ route('products') }}?category={{ $category }}" class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-orange-50 text-orange-700 border border-orange-100 hover:bg-orange-100 transition">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
                        </svg>
                        <span class="text-sm font-semibold">{{ $category }}</span>
                    </a>
                    @endforeach
                </div>
            </div>
        </div>
        @endif

        <!-- Main Content -->
        <div class="max-w-7xl mx-auto px-4 py-8">
        <!-- Main Content -->
        <div class="max-w-7xl mx-auto px-4 py-8">
            <!-- Main Content -->
            <div class="max-w-7xl mx-auto px-4 py-8">
                    <div>
                        <p class="text-sm font-semibold text-gray-900">Voucher & Cashback</p>
                        <p class="text-xs text-gray-600">Nikmati potongan harga harian</p>
                            <a href="{{ route('products') }}" class="text-orange-700 hover:text-orange-800 font-semibold text-sm">Lihat Semua →</a>
                </div>
                <div class="flex items-center gap-3 bg-emerald-50 border border-emerald-100 rounded-xl px-4 py-3">
                    <div class="bg-emerald-500 text-white w-10 h-10 flex items-center justify-center rounded-full text-lg font-bold">🚚</div>
                    <div>
                        <p class="text-sm font-semibold text-gray-900">Gratis Ongkir</p>
                        <p class="text-xs text-gray-600">Khusus produk bertanda hijau</p>
                    </div>
                </div>
                <div class="flex items-center gap-3 bg-sky-50 border border-sky-100 rounded-xl px-4 py-3">
                    <div class="bg-sky-500 text-white w-10 h-10 flex items-center justify-center rounded-full text-lg font-bold">🛡️</div>
                    <div>
                        <p class="text-sm font-semibold text-gray-900">Pembayaran Aman</p>
                        <p class="text-xs text-gray-600">Proteksi transaksi SentriPay</p>
                    </div>
                </div>
            </div>

            <!-- Flash Sale Section -->
            @if($featuredProducts->count() > 0)
            <div class="mb-12">
                <div class="flex items-center justify-between mb-4">
                    <div class="flex items-center gap-3">
                        <span class="inline-flex items-center gap-2 bg-orange-500 text-white px-3 py-1 rounded-lg text-sm font-semibold">
                            ⚡ Flash Sale
                        </span>
                        <div x-data="{ end: Date.now() + 7200000, remaining() { return Math.max(0, Math.floor((this.end - Date.now())/1000)); } }" class="flex items-center gap-2 text-sm text-gray-700">
                            <span class="text-gray-700 font-semibold">Berakhir dalam</span>
                            <span class="font-mono bg-orange-600 text-white px-3 py-1 rounded-lg font-bold" x-text="String(Math.floor(remaining()/3600)).padStart(2,'0') + ':' + String(Math.floor((remaining()%3600)/60)).padStart(2,'0') + ':' + String(remaining()%60).padStart(2,'0')"></span>@@
                        </div>
                    </div>
                    <a href="{{ route('products') }}" class="text-orange-600 hover:text-orange-700 font-medium text-sm">Lihat Semua →</a>
                </div>
                <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4">
                    @foreach($featuredProducts as $product)
                    <div class="bg-white border border-orange-100 rounded-xl overflow-hidden hover:shadow-lg transition group">
                        <div class="relative">
                            @if($product->image_path)
                                <img src="{{ asset('storage/' . $product->image_path) }}" alt="{{ $product->name }}" class="w-full h-40 object-cover">
                            @else
                                <div class="w-full h-40 bg-gradient-to-br from-orange-50 to-orange-100 flex items-center justify-center">
                                    <svg class="w-12 h-12 text-orange-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                    </svg>
                                </div>
                            @endif
                            <div class="absolute top-2 left-2 bg-red-500 text-white text-xs font-bold px-2 py-1 rounded">Flash</div>
                            <div class="absolute bottom-2 left-2 bg-white/90 text-orange-600 text-xs font-semibold px-2 py-1 rounded">Hemat 10%</div>
                        </div>
                        <div class="p-3">
                            <h3 class="text-sm font-semibold text-gray-900 mb-1 line-clamp-1">{{ $product->name }}</h3>
                            <p class="text-xs text-gray-500 mb-2 line-clamp-2">{{ $product->description }}</p>
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-lg font-bold text-orange-600">Rp {{ number_format($product->price, 0, ',', '.') }}</span>
                                <span class="text-xs text-gray-400 line-through">Rp {{ number_format($product->price * 1.1, 0, ',', '.') }}</span>
                            </div>
                            <div class="flex items-center justify-between text-xs text-gray-500 mb-2">
                                <span>Terjual {{ $product->sold }}</span>
                                <span>Stok {{ $product->stock }}</span>
                            </div>
                            <a href="{{ route('checkout', $product->id) }}" class="block w-full bg-orange-500 text-white text-center py-2 rounded-lg hover:bg-orange-600 transition text-sm font-semibold">Beli</a>
                        </div>
                    </div>
                    @endforeach
                </div>
            </div>
            @endif

            <!-- Featured Products -->
            @if($featuredProducts->count() > 0)
            <div class="mb-12">
                <div class="flex items-center justify-between mb-6">
                        <h2 class="text-2xl font-bold text-gray-900">🔥 Produk Terlaris</h2>
                        <a href="{{ route('products') }}" class="text-orange-700 hover:text-orange-800 font-semibold">
                        Lihat Semua →
                    </a>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                    @foreach($featuredProducts as $product)
                    <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-2xl transform hover:-translate-y-1 transition group">
                        <div class="relative">
                            @if($product->image_path)
                                <img src="{{ asset('storage/' . $product->image_path) }}" alt="{{ $product->name }}" class="w-full h-56 object-cover">
                            @else
                                <div class="w-full h-56 bg-gradient-to-br from-blue-100 to-purple-100 flex items-center justify-center">
                                    <svg class="w-20 h-20 text-blue-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                    </svg>
                                </div>
                            @endif
                            <div class="absolute top-2 right-2 bg-red-500 text-white px-3 py-1 rounded-full text-xs font-bold">
                                HOT
                            </div>
                        </div>
                        <div class="p-5">
                            <h3 class="font-bold text-gray-900 text-lg mb-2 line-clamp-1">{{ $product->name }}</h3>
                            <p class="text-gray-600 text-sm mb-3 line-clamp-2">{{ $product->description }}</p>
                            <div class="flex items-center justify-between mb-4">
                                <span class="text-2xl font-bold text-orange-600">Rp {{ number_format($product->price, 0, ',', '.') }}</span>
                                <span class="text-xs text-gray-500 bg-gray-100 px-2 py-1 rounded">Terjual {{ $product->sold }}</span>
                            </div>
                            <a href="{{ route('checkout', $product->id) }}" class="block w-full bg-gradient-to-r from-orange-500 to-orange-600 text-white text-center py-3 rounded-lg hover:from-orange-600 hover:to-orange-700 transition font-semibold group-hover:scale-105 transform">
                                Beli Sekarang
                            </a>
                        </div>
                    </div>
                    @endforeach
                </div>
            </div>
            @endif

            <!-- All Available Products -->
            <div class="mb-12">
                <div class="flex items-center justify-between mb-6">
                        <h2 class="text-2xl font-bold text-gray-900">🛍️ Semua Produk</h2>
                        <a href="{{ route('products') }}" class="text-orange-700 hover:text-orange-800 font-semibold">
                        Jelajahi Lebih →
                    </a>
                </div>
                @if($availableProducts->count() > 0)
                    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                        @foreach($availableProducts as $product)
                        <div class="bg-white rounded-lg shadow hover:shadow-xl overflow-hidden transition group">
                            <div class="relative">
                                @if($product->image_path)
                                    <img src="{{ asset('storage/' . $product->image_path) }}" alt="{{ $product->name }}" class="w-full h-48 object-cover">
                                @else
                                    <div class="w-full h-48 bg-gradient-to-br from-gray-100 to-gray-200 flex items-center justify-center">
                                        <svg class="w-16 h-16 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                        </svg>
                                    </div>
                                @endif
                            </div>
                            <div class="p-4">
                                <h3 class="font-semibold text-gray-900 mb-1 line-clamp-1">{{ $product->name }}</h3>
                                <div class="flex items-center text-xs text-gray-500 mb-2">
                                    <svg class="w-3 h-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                    </svg>
                                    {{ $product->seller->name }}
                                </div>
                                <div class="flex justify-between items-center mb-3">
                                    <span class="text-lg font-bold text-orange-600">Rp {{ number_format($product->price, 0, ',', '.') }}</span>
                                    <span class="text-xs text-gray-500">Stok: {{ $product->stock }}</span>
                                </div>
                                <a href="{{ route('checkout', $product->id) }}" class="block w-full bg-orange-500 text-white text-center py-2 rounded-lg hover:bg-orange-600 transition text-sm font-medium">
                                    Beli
                                </a>
                            </div>
                        </div>
                        @endforeach
                    </div>
                @else
                    <div class="text-center py-20 bg-gray-50 rounded-xl">
                        <svg class="w-24 h-24 mx-auto mb-4 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"></path>
                        </svg>
                        <p class="text-gray-500 text-lg">Belum ada produk yang tersedia</p>
                    </div>
                @endif
            </div>

            <!-- My Products Section (for sellers) -->
            @if($myProducts->count() > 0)
                <div class="bg-white border-t-4 border-green-500 rounded-xl shadow-sm p-8 mb-12">
                <div class="flex items-center justify-between mb-6">
                    <div>
                            <h2 class="text-2xl font-bold text-gray-900">📦 Produk Saya</h2>
                        <p class="text-gray-600 mt-1">Kelola dan pantau produk yang Anda jual</p>
                    </div>
                    <a href="{{ route('seller.products') }}" class="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition font-semibold">
                        Kelola Produk
                    </a>
                </div>
                <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
                    @foreach($myProducts as $product)
                    <div class="bg-white rounded-lg shadow overflow-hidden hover:shadow-lg transition">
                        @if($product->image_path)
                            <img src="{{ asset('storage/' . $product->image_path) }}" alt="{{ $product->name }}" class="w-full h-32 object-cover">
                        @else
                            <div class="w-full h-32 bg-gray-200 flex items-center justify-center">
                                <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                </svg>
                            </div>
                        @endif
                        <div class="p-3">
                            <h3 class="font-semibold text-sm text-gray-900 mb-1 line-clamp-1">{{ $product->name }}</h3>
                            <div class="flex justify-between items-center">
                                <span class="text-xs font-bold text-gray-900">Rp {{ number_format($product->price, 0, ',', '.') }}</span>
                                <span class="text-xs text-gray-500">{{ $product->stock }}</span>
                            </div>
                        </div>
                    </div>
                    @endforeach
                </div>
            </div>
            @endif

            <!-- Quick Stats -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div class="bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl shadow-lg p-6 text-white">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-blue-100 text-sm font-medium mb-1">Total Pesanan</p>
                            <p class="text-4xl font-bold">{{ $totalOrders }}</p>
                        </div>
                        <div class="bg-white/20 p-4 rounded-full">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
                            </svg>
                        </div>
                    </div>
                </div>

                <div class="bg-gradient-to-br from-green-500 to-green-600 rounded-xl shadow-lg p-6 text-white">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-green-100 text-sm font-medium mb-1">Total Belanja</p>
                            <p class="text-3xl font-bold">Rp {{ number_format($totalPurchases, 0, ',', '.') }}</p>
                        </div>
                        <div class="bg-white/20 p-4 rounded-full">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                        </div>
                    </div>
                </div>

                <div class="bg-gradient-to-br from-purple-500 to-purple-600 rounded-xl shadow-lg p-6 text-white">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-purple-100 text-sm font-medium mb-1">Saldo Akun</p>
                            <p class="text-3xl font-bold">Rp {{ number_format($balance, 0, ',', '.') }}</p>
                        </div>
                        <div class="bg-white/20 p-4 rounded-full">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h10m4 0a1 1 0 100-2 1 1 0 000 2zM7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                            </svg>
                        </div>
                </div>
            </div>
        </div>
    @endif
</div>
